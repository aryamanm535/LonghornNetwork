import java.io.*;
import java.util.*;

/**
 * Utility class responsible for reading student data from input files and
 * constructing {@link UniversityStudent} objects for use in the Longhorn
 * Network.
 */
public class DataParser {
    /**
     * Parses the given file and creates a list of {@link UniversityStudent}s
     *
     * @param filename the path to the input file that contain student data
     * @return a list of students parsed from the file; the list may be empty
     *         (if no valid students)
     * @throws IOException if an I/O error occurs.
     */
    public static List<UniversityStudent> parseStudents(String filename) throws IOException {
        return new ArrayList<>();
    }
}
