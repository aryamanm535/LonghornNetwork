import java.util.*;

/**
 * Implements the Gale–Shapley matching algorithm used to assign compatible 
 * roommates in the Longhorn Network.
 */
public class GaleShapley {
    /**
     * Runs the Gale–Shapley algorithm on the given list of students and
     * updates their roommate assignments to produce stable matching.
     *
     * @param students list of students that should be matched with roommates;
     * (even number of elements expected)
     */
    public static void assignRoommates(List<UniversityStudent> students) {
    }
}
